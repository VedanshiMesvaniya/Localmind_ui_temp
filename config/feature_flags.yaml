# Feature flags for SQL Pipeline Optimization
# Production configuration — all optimizations enabled.
# This file takes priority over features.yaml in the loading order.

features:
  delta_repair_enabled: true
  token_budget_enabled: true
  schema_compaction_enabled: true
  sql_safety_enabled: true
  zero_row_handling_enabled: true
  fast_path_enabled: true
  provider_routing_v2_enabled: true
